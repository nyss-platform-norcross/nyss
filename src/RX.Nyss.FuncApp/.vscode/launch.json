{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run RX.Nyss.FuncApp",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}