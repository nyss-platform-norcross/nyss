import { strings, stringKeys } from "../../../strings";

export const headManagerConsentsSiteMap = [
  {
    path: "/headManagerConsents",
    title: () => strings(stringKeys.headManagerConsents.title)
  }
];
